import { SharedValue } from 'react-native-reanimated';

export function useScreenScroll(_scrollY: SharedValue<number>, _shouldAnimateHalo: boolean, _options?: { isNearBottom?: SharedValue<boolean> }) {
  // Mock implementation - does nothing in tests
}
