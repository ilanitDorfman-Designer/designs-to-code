// Main component + API types (EtTopPanel comes via ./api)
export * from './api';

// Shell-facing constants (the rest of ./constants stays internal)
export { TOP_PANEL_HEIGHT_REGULAR, TOP_PANEL_HEIGHT_TALL } from './constants';
