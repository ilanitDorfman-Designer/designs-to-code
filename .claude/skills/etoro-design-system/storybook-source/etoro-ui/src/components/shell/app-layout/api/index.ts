export { EtAppLayout } from '../et-app-layout';

// Public types only — internal slot-classification/animation contract types stay in './types'.
export type {
  AppLayoutAsideTrigger,
  EtAppLayoutAsideProps,
  EtAppLayoutMainProps,
  EtAppLayoutProps,
  EtAppLayoutSideMenuProps,
  EtAppLayoutTopPanelProps,
} from './types';
