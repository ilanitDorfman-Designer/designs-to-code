export interface ToriBadgeLabelProps {
  /** The neutral half ("Ask") — the gradient starts on it in carbon900. */
  label: string;
  /** The brand half ("Tori") — past the gradient stop, so solid verdictPositive600. */
  accentLabel: string;
}
