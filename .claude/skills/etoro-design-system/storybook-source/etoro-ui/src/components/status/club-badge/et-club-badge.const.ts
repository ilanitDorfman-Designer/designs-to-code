/** Figma `club badge` (`4712:50905`) outer height. */
export const CLUB_BADGE_HEIGHT = 18;

/** Figma `club badge` border width. */
export const CLUB_BADGE_BORDER_WIDTH = 1.5;

/** Figma `club badge` horizontal padding (`4712:50905`). */
export const CLUB_BADGE_PADDING_LEFT = 3;
export const CLUB_BADGE_PADDING_RIGHT = 4;

/** Figma `lock-fill` icon size (`4712:50906`). */
export const CLUB_BADGE_LOCK_SIZE = 10;
/** Optical nudge — lock sits high in its slot relative to the italic wordmark. */
export const CLUB_BADGE_LOCK_OFFSET_Y = -1;

/** Figma `club wordmark` typography (`4847:14763`) — Playfair substitutes Larken. */
export const CLUB_BADGE_WORDMARK_FONT_FAMILY = 'PlayfairDisplay-MediumItalic';
export const CLUB_BADGE_WORDMARK_FONT_SIZE = 10;
export const CLUB_BADGE_WORDMARK_LINE_HEIGHT = 10;
export const CLUB_BADGE_WORDMARK_LETTER_SPACING = -0.1;
/** Optical nudge — italic cap height sits high in the em-box. */
export const CLUB_BADGE_WORDMARK_OFFSET_Y = 0;

/** Figma secondary background blur radius (~4px). */
export const CLUB_BADGE_BLUR_INTENSITY = 8;
