import { X5 } from '../../../core/styles/spacing';

/**
 * Uniform content padding for medium / large cards (Figma).
 * Header, content, and footer all use `X5` (20px) — there is no
 * small / medium density variant.
 */
export const MEDIA_CARD_PADDING = X5;
