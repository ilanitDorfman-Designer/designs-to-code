import { X1, X2, X6 } from '../../../../../core/styles/spacing';

/**
 * Header horizontal padding.
 * Uses X6 (24px) from design system spacing.
 */
export const HEADER_PADDING_HORIZONTAL = X6; // 24px

/**
 * Header top padding.
 */
export const HEADER_PADDING_TOP = 0;

/**
 * Header bottom padding.
 * Uses X3 (12px) from design system spacing.
 */
export const HEADER_PADDING_BOTTOM = X6;

/**
 * Minimum header height for consistent touch targets.
 */
export const HEADER_MIN_HEIGHT = 44;

export const ACTION_HIT_SLOP = X2;
export const ACTION_PADDING = X1;
export const ACTIONS_AREA_MARGIN_LEFT = X2;
