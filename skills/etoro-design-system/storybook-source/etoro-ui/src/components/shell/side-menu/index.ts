// Main components + API types (EtSideMenu / EtSideMenuTrigger come via ./api)
export * from './api';

// Shell-facing constants (the rest of ./constants stays internal)
export { PANEL_WIDTH, RAIL_WIDTH_BY_TIER, railWidthForTier, WORDMARK_GLYPH_HEIGHT } from './constants';
