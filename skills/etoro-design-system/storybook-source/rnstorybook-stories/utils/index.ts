// Export simple CodeBlock for intro stories (uses Alert-free copy feedback)
export { CodeBlock as SimpleCodeBlock } from './code-block';

// Export all from storybook-template (includes its own CodeBlock with theme system)
export * from './storybook-template';
